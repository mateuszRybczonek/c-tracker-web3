export const enum EInputType {
  text = 'text',
  email = 'email',
  url = 'url',
  password = 'password',
  number = 'number',
  tel = 'tel'
}

export enum EButtonType {
  none = -1,
  primary,
  secondary,
  tertiary
}

export enum EButtonStyle {
  normal,
  warning,
  danger
}
